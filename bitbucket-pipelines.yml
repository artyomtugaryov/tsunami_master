image: gcc:6.1

pipelines:
  default:
    - step:
        script:
          - apt-get update
          - apt-get install -y cmake
          - mkdir build_tmlib
          - cd build_tmlib
          - ls -la ../src
